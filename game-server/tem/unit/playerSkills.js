var EventEmitter = require('events').EventEmitter;
var util = require('util');

module.exports = playerSkills;

util.inherits(playerSkills, EventEmitter);

var playerSkills = function(data) {
	//初始化这个inventory 容器
	this.uid=data.uid;
	








}